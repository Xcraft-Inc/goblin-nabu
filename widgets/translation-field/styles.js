//T:2019-02-27

export default function styles() {
  const bottomLine = {
    border: 'hidden',
    borderBottom: 'solid thin #c1d1e0',
    width: '100%',
  };

  const showIcuButton = {
    marginBottom: '15px',
    position: 'relative',
    left: '120px',
    marginRight: '120px',
  };

  return {
    bottomLine,
    showIcuButton,
  };
}
