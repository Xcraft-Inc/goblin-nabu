/******************************************************************************/

export default function styles(theme, props) {
  const container = {
    width: '100%',
    overflow: 'auto',
  };

  const table = {
    borderCollapse: 'collapse',
  };

  const item = {
    padding: '8px',
  };

  return {container, table, item};
}

/******************************************************************************/
